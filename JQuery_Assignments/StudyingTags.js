/*STUDY ONLY THESE TAGS!!!

Effects (functions to do some cool animation effects)
.hide() - Hide the matched elements.
.show() - Display the matched elements.
.toggle() - Bind two or more handlers to the matched elements,
to be executed on alternate clicks
.slideDown() - Display the matched elements with a sliding motion.
.slideToggle() -  Display or hide the matched elements with a sliding motion.
.slideUp() - Hide the matched elements with a sliding motion.
.fadeOut() - Hide the matched elements by fading them to transparent (fade into invis).
.fadeIn() - Display the matched elements by fading them into opaque. (fade in from vanish).

CSS (adding or removing a class for any HTML element/DOM)
.addClass() - Adds the specified class(es) to each element in the set of matched elements.
.removeClass() - Remove a single class, multiple classes, or all classes from each element
in the set of matched elements.
.css() - Get the value of a computed style property for the first element in the
set of matched elements or set one or more CSS properties for every matched element.
Contents:
  .css( propertyName )
  .css( propertyName )
  .css( propertyNames )
  .css( propertyName, value )
  .css( propertyName, value )
  .css( propertyName, function )
  .css( properties )

Manipulation (retrieving or setting value or text in any HTML element)
.after() - Insert content, specified by the parameter, after each element
in the set of matched elements
.append() - Insert content, specified by the parameter, to the end of
each element in the set of matched elements.
.prepend() - Insert content, specified by the parameter, to the
beginning of each element in the set of matched elements.
.attr()
.before()
.html()
.text()
.val()

Events (functions to handle an event)
.click()
.on()

.live() - deprecated (JQuery 1.7)
.hover()
*/
